# Data Dictionary — `data/combined_data.json`

ชุดข้อมูลรวม 3 ปี (470 รายการ) · โครงสร้างเดียวกันกับ `data/sheets/08_Combined_Data.json`, `data/all_points.geojson` (properties), และ `data.xlsx` ชีต `08_Combined Data` · ปรับปรุงล่าสุด 10 ก.ค. 2569

| Field | ชนิด | คอลัมน์ต้นทาง (ไฟล์รวม 3 ปี) | คำอธิบาย |
|---|---|---|---|
| `year` | number | (ชื่อชีต) | ปีงบประมาณ พ.ศ. — 2566 / 2567 / 2568 |
| `point_id` | number | รหัสจุด | ลำดับจุดภายในปีนั้น (เริ่ม 1 ใหม่ทุกปี — อ้างอิงข้ามปีด้วยพิกัด ไม่ใช่รหัส) |
| `point_name` | string | ชื่อจุด/บริเวณ | ชื่อจุดมาตรฐาน (ตรวจทานรอบ 10 ก.ค. 2569) |
| `point_type` | string | ประเภทปัญหา | เช่น ทางแยก · คอขวด · ป้ายรถเมล์ · ช่วงถนน/จุดกลับรถ |
| `district` | string | เขต | เขตปกครองของ กทม. (จุดกว้างอาจคาบเกี่ยวสองเขต เช่น วงเวียนใหญ่) |
| `zone` | string | กลุ่มเขต/โซน | กรุงเทพกลาง/เหนือ/ใต้/ตะวันออก/กรุงธนเหนือ/กรุงธนใต้ |
| `road` | string | ถนน | ถนนหลักของจุด (บางจุดมีหลายเส้น คั่นด้วย `/`) |
| `cause` | string \| null | สาเหตุเบื้องต้น | ข้อความอิสระ — มีเฉพาะปี 2566 |
| `peak_time` | string \| null | [เฉพาะปี] ช่วงเวลาที่การจราจรติดขัด | มีเฉพาะปี 2566 |
| `agency_main` | string | หน่วยงานรับผิดชอบหลัก | กทม. หรือหน่วยงานนอกอำนาจ กทม. (ทล. · ทช. · กทพ. · รฟท. · เอกชน) |
| `solution_group` | string \| null | กลุ่มแนวทางแก้ไข | แนวทางแก้ไขหลักของจุด |
| `progress_pct` | number \| null | ความคืบหน้า/สถานะ | 0–100 (milestone-based) — **null ทั้งหมดในปี 2566** (ยังไม่มีระบบติดตาม) |
| `sol_traffic_mgmt` | 1/0/null | [เฉพาะปี] บริหารจัดการระบบจราจร… | แผนใช้มาตรการบริหารจัดการจราจร (เฉพาะ 2567–2568) |
| `sol_enforcement` | 1/0/null | [เฉพาะปี] กวดขันวินัยจราจร | แผนใช้มาตรการกวดขันวินัยจราจร |
| `sol_signal` | 1/0/null | [เฉพาะปี] ปรับ/ติดตั้งสัญญาณไฟจราจร | แผนปรับหรือติดตั้งสัญญาณไฟ |
| `sol_physical` | 1/0/null | [เฉพาะปี] ปรับลักษณะกายภาพ | แผนปรับกายภาพถนน/ทางแยก |
| `sol_busbay` | 1/0/null | [เฉพาะปี] จัดทำ Bus Bay หรือ Bus Priority | แผนจัดทำ Bus Bay / Bus Priority |
| `sol_cctv` | 1/0/null | [เฉพาะปี] ติดกล้อง CCTV | แผนติดตั้งกล้อง CCTV |
| `latitude` | number | ละติจูด | WGS84 — จุดที่ geocode จาก landmark เดียวกันอาจมีพิกัดตรงกันแม้เป็นคนละจุด (ดู METHODOLOGY) |
| `longitude` | number | ลองจิจูด | WGS84 |

หมายเหตุ:

- `all_points.geojson`: properties มีทุก field ยกเว้น `latitude`/`longitude` (อยู่ใน `geometry.coordinates` เป็น `[lon, lat]`) — **ห้ามเพิ่ม lat/lon เข้า properties**
- สถานีตำรวจและ Overlap อยู่ในไฟล์ต้นทาง ยังไม่ถูกนำเข้าชั้นข้อมูลนี้ (คงโครงสร้างเดิมเพื่อความเข้ากันได้ของ dashboard)
- เกณฑ์สถานะจาก `progress_pct` (ยืนยัน 10 ก.ค. 2569): `100` = เสร็จสิ้น · `1–99` = อยู่ระหว่างดำเนินการ · `0` = ติดปัญหาอุปสรรค

---
© Prapawadee_W. · Traffic and Transportation Department, BMA
